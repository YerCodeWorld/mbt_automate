# A way to create the corresponding files when wu used canva bulk create

#elif c == "write":
#hd_type = command[1][-1]
#ACTIONS[c](path, f"{command[1]}", data[cmds[0]][cmds[1]], HEADER_TYPE[hd_type])


# same with pdfs

# if c == "pdfs":
# ACTIONS[c](data, AVAILABLE_COMMANDS[command[1]], command[1], path)